apiVersion: v1
kind: Service
metadata:
  name: {{ include "datacenter.fullname" . }}
  labels:
    {{- include "datacenter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datacenter.selectorLabels" . | nindent 4 }}
