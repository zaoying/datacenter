server:
  port: 8081
spring:
  application:
    name: school-service
  datasource:
    dirver: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://devops2/cloudtest?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  zipkin:
    base-url: http://localhost:8060
  sleuth:
    sampler:
      percentage: 1.0
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true